@model IEnumerable<ASM.Models.OrderDetail>

@{
    ViewData["Title"] = "Chi tiết đơn hàng";
}

    <h1 class="text-primary"><i class="fas fa-info-circle"></i> Chi tiết đơn hàng</h1>

@*<p>
    <a asp-action="Create">Create New</a>
</p>*@
<table class="table">
    <thead>
        <tr>
            <th>
                @Html.DisplayNameFor(model => model.Food.Name)
            </th>
            <th>
                @Html.DisplayNameFor(model => model.Food.Image)
            </th>
            <th>
                @Html.DisplayNameFor(model => model.Quantity)
            </th>
            <th>
                @Html.DisplayNameFor(model => model.IntoMoney)
            </th>
            <th>
                @Html.DisplayNameFor(model => model.Note)
            </th>
            @*<th>
                @Html.DisplayNameFor(model => model.Order)
            </th>
            <th></th>*@
        </tr>
    </thead>
    <tbody>
        @foreach (var item in Model) {
            <tr>
                <td>
                @Html.DisplayFor(modelItem => item.Food.Name)
                </td>
                <td>
                    <img src="~/images/food/@item.Food.Image" style="width:150px" />
                    @*@Html.DisplayFor(modelItem => item.Food.Image)*@
                </td>
                <td>
                    @Html.DisplayFor(modelItem => item.Quantity)
                </td>
                <td>
                    @Html.DisplayFor(modelItem => item.IntoMoney)
                </td>
                <td>
                    @Html.DisplayFor(modelItem => item.Note)
                </td>
                @*<td>
                    @Html.DisplayFor(modelItem => item.Order.OrderID)
                </td>*@
                @*<td>
                    <a asp-action="Edit" asp-route-id="@item.DetailID">Edit</a> |
                    <a asp-action="Details" asp-route-id="@item.DetailID">Details</a> |
                    <a asp-action="Delete" asp-route-id="@item.DetailID">Delete</a>
                </td>*@                                                             
            </tr>   
        }
    </tbody>
</table>
